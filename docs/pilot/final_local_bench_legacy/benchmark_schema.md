# Final Local Benchmark Schema

Raw CSV rows identify timestamp, git commit, method, base-OT mode, field parameters, trial index, warmup flag, status, data source, correctness, timing, communication, and memory fields. `data_source` is one of `measured`, `modeled`, or `skipped`; modeled direct-baseline values are stored separately from measured values.
