# Split Backend Recommendation

Correctness passed for all three split-only backends in the local TCP comparison. The selected optimized backend is `SPLIT_COEFF_OT` because it is the lower-communication measured backend on the tested split-only points; silent R2C is retained as an alternative exact silent-VOLE conversion.
